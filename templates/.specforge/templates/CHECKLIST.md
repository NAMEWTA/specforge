---
name: '{{artifactName}}'
type: 'checklist'
phase: 'quality'
version: '{{version}}'
createdAt: '{{createdAt}}'
---

# 质量检查清单：{{featureName}}

> 本清单用于 `quality` 阶段的自检与交叉评审。无新验证证据，禁止声称完成（见 E004_noVerificationEvidence）。

## 使用说明

- 每一项的状态为：`[ ]` 未验证 / `[x]` 已通过 / `[-]` 进行中 / `N/A` 不适用。
- 每个 `[x]` 必须附有**验证证据**（命令输出、测试编号、链接等）。
- 若填写 `N/A`，必须说明原因。

## 1. 需求与设计一致性

- [ ] 所有验收标准（AC）均有对应的测试或检查点
  - 证据：
- [ ] 接口契约与代码实现一致（参数、返回值、错误码）
  - 证据：
- [ ] 设计文档中的错误分类已在代码中落实
  - 证据：
- [ ] 需求变更已同步到 proposal / design，版本号已更新
  - 证据：

## 2. 代码质量

- [ ] Lint 通过（`pnpm lint` 或对应语言适配器命令）
  - 证据：
- [ ] 格式化通过（`pnpm format --check` 或等价命令）
  - 证据：
- [ ] 无 `TODO` / `FIXME` 未登记到任务列表
  - 证据：
- [ ] 无被注释掉的死代码
  - 证据：
- [ ] 公开 API 均有文档注释
  - 证据：

## 3. 测试覆盖

- [ ] 单元测试通过（`pnpm test`）
  - 证据：
- [ ] 关键路径覆盖率达标（项目阈值）
  - 证据：
- [ ] 新增 / 修改的公共接口均有测试
  - 证据：
- [ ] 边界条件已覆盖（空值、极值、并发、异常）
  - 证据：
- [ ] 属性测试（PBT）已覆盖核心不变量（若适用）
  - 证据：
- [ ] 回归测试无失败
  - 证据：

## 4. 构建与集成

- [ ] 构建通过（`pnpm build`）
  - 证据：
- [ ] 集成测试通过
  - 证据：
- [ ] 关键外部依赖版本已锁定
  - 证据：

## 5. 安全与合规

- [ ] 无已知高危漏洞（依赖审计通过）
  - 证据：
- [ ] 敏感信息（密钥、token）未提交到仓库
  - 证据：
- [ ] 鉴权与权限边界已测试
  - 证据：
- [ ] 输入校验覆盖所有外部入口
  - 证据：

## 6. 可观测性

- [ ] 关键路径有日志（结构化、脱敏）
  - 证据：
- [ ] 错误路径有告警 / 指标
  - 证据：
- [ ] 监控面板可显示本次变更的关键指标
  - 证据：

## 7. 文档与交付

- [ ] README / CHANGELOG 已更新
  - 证据：
- [ ] 迁移指南（如适用）已完成
  - 证据：
- [ ] 运行手册（runbook）已覆盖回滚步骤
  - 证据：

## 评审结论

- 评审人：
- 评审日期：
- 发现的问题：
- 是否放行：是 / 否
