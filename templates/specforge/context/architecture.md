# 项目架构（Structure）

> 本文件是项目级常驻知识的 **structure 层**，记录模块图、依赖规则、ADR 列表、跨模块契约、扩展点与容量边界。
> 由 `specforge init` 首建；后续由 `evolution-retrospect` promote DESIGN § 9 条目更新。
> AI 代理在 `design-explore` 阶段 grep 本文件以对齐既有架构。

---

## 模块图

<!-- 用 Mermaid 或文本描述项目的顶层模块划分与依赖方向 -->

```mermaid
flowchart TB
  %% 在此绘制项目模块关系图
```

---

## 依赖规则

<!-- 记录模块间的依赖方向约束，禁止的依赖路径等 -->

| 规则 | 说明 | 违例后果 |
|------|------|---------|
| | | |

---

## ADR 列表

<!-- 架构决策记录（Architecture Decision Records），每条重大技术决策一行 -->

| ADR 编号 | 日期 | 决策标题 | 状态 | 备注 |
|----------|------|---------|------|------|
| | | | | |

---

## 跨模块契约

<!-- 记录模块间的接口契约、协议约定、数据格式等 -->

| 契约名称 | 涉及模块 | 契约内容 | 变更条件 |
|----------|---------|---------|---------|
| | | | |

---

## 扩展点

<!-- 记录项目预留的扩展机制，供后续 change 复用 -->

| 扩展点 | 所在路径 | 扩展方式 | 约束 |
|--------|---------|---------|------|
| | | | |

---

## 容量边界

<!-- 记录已知的性能 / 规模边界，避免 AI 代理在设计时超出限制 -->

| 边界项 | 当前值 | 上限 | 触发后果 | 备注 |
|--------|-------|------|---------|------|
| | | | | |
